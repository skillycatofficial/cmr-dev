# ─── Sanity CMS ──────────────────────────────────────────────────────────────
# Get these from https://www.sanity.io/manage → your project → Settings → API

NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: for server-side mutations / webhooks (never expose to browser)
# SANITY_API_TOKEN=your_token_here

# ─── reCAPTCHA v3 (spam protection on Contact/Enquiry/Careers forms) ─────────
# Get these from https://www.google.com/recaptcha/admin — register the site
# domain, choose reCAPTCHA v3. Forms work without these set; spam checking
# only activates once both are present.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_site_key_here
RECAPTCHA_SECRET_KEY=your_secret_key_here

# ─── Google Tag Manager + Search Console (site verification) ────────────────
# GTM container ID from https://tagmanager.google.com. GA4 (and any other
# pixels — Ads, Meta, etc.) get configured as tags inside this GTM container
# by marketing directly, no code changes needed. Leave unset to disable.
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# HTML tag verification token from https://search.google.com/search-console
# → Add Property → cmrdevelopers.com → HTML tag method. Paste only the
# "content" value from the meta tag, not the whole tag.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_verification_token_here
